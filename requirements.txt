# Local development dependencies for Kestra Aegis
# Note: Kestra tasks run in Docker containers and install these via beforeCommands,
# but these are required locally for IDE autocomplete, linting, and utility scripts.

duckdb==1.3.0
google-genai==1.14.0
sqlglot==26.30.0
requests==2.32.3
